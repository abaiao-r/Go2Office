@startuml settings-validation
!theme plain

start
:User inputs\nsettings;

if (Days per week\n1-5?) then (no)
    :Error: "Must be 1-5";
    stop
endif

if (Hours per day\n1-12?) then (no)
    :Error: "Must be 1-12";
    stop
endif

if (5 unique\nweekdays?) then (no)
    :Error: "Must order\nall 5 weekdays";
    stop
endif

:Save settings ✅;
stop

@enduml

